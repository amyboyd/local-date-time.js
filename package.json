{
  "name": "local-date-time",
  "version": "0.0.8",
  "dependencies": {},
  "devDependencies": {
    "babel-core": "^6.24.1",
    "babel-loader": "^6.4.1",
    "babel-preset-env": "^1.4.0",
    "chai": "^3.5.0",
    "jshint": "^2.9.5",
    "mocha": "^3.2.0",
    "webpack": "^2.4.1"
  },
  "scripts": {
    "test": "bin/build && bin/test",
    "test-watch": "bin/build && bin/test --watch",
    "prepublish": "bin/build && bin/test && bin/build && echo 'Waiting 5 seconds to give you a chance to stop!' && sleep 5"
  },
  "main": "dist/local-date-time.commonjs2.js",
  "author": "Amy Boyd <amy@amyboyd.co.uk>",
  "engines": {
    "node": ">=6.0.0",
    "npm": ">=3.0.0"
  }
}
